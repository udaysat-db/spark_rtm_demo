resources:
  secret_scopes:
    kafka:
      name: ${var.kafka_secret_scope}
      # The bundle provisions the (empty) scope only. Add the values out of band:
      #   databricks secrets put-secret ${var.kafka_secret_scope} sasl_jaas_config
      #   databricks secrets put-secret ${var.kafka_secret_scope} bootstrap_servers
      # Never commit Kafka credentials.
